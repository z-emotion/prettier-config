# z-emotion prettier config

- ref https://prettier.io/docs/en/configuration.html#sharing-configurations

# usage

- add to package.json(top level)

```
{
    ...
    "prettier": "@z-emotion/prettier-config",
    ...
}
```

- add dev package

`npm i @z-emotion/prettier-config -D`
